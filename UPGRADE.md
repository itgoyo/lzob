# 升级指南

如果你已经安装了旧版本的系统，请按照以下步骤升级到 v2.0.0。

## 升级步骤

### 1. 安装新的依赖包

```bash
npm install
```

这将自动安装新增的依赖：
- `jsonwebtoken` - JWT 认证
- `bcryptjs` - 密码加密
- `nodemailer` - 邮件发送
- 以及对应的 TypeScript 类型定义

### 2. 更新环境变量

编辑 `.env` 文件，添加以下新的配置：

```env
# 管理员账号（必填）
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="your-secure-password"

# JWT密钥（必填，生产环境请使用复杂随机字符串）
JWT_SECRET="your-jwt-secret-change-this-in-production"
```

**⚠️ 重要**：
- `ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 是登录系统的账号密码
- `JWT_SECRET` 用于生成和验证 JWT token，请使用复杂的随机字符串
- 生产环境务必修改这些值！

### 3. 更新数据库结构

运行以下命令更新数据库，这将添加新的 `email_configs` 表：

```bash
npm run db:push
```

这个命令会：
- 创建新的 `email_configs` 表用于存储邮箱配置
- 保留所有现有数据

### 4. 重启服务

```bash
# 开发环境
npm run dev

# 生产环境（如果使用 PM2）
pm2 restart luzhi-admin
```

### 5. 首次登录

访问 [http://localhost:3000](http://localhost:3000)，系统会自动重定向到登录页面。

使用你在 `.env` 文件中配置的账号密码登录。

## 新功能使用

### 登录认证

- 所有页面现在都需要登录才能访问
- Token 有效期为 7 天
- 可以点击右上角"退出登录"按钮退出

### 邮箱通知

1. 登录后进入"设置"页面
2. 切换到"邮箱通知"标签
3. 点击"新增配置"
4. 填入邮箱和 SMTP 配置信息
5. 保存后即可接收到期提醒邮件

### 站内提醒

- 有主播到期时，系统会在页面顶部显示红色提醒横幅
- 可以单独关闭某个主播的提醒，或一键关闭所有提醒
- 点击"查看详情"可快速跳转到已到期页面

## 安全建议

1. **修改默认密码**：首次登录后请立即在 `.env` 文件中修改 `ADMIN_PASSWORD`
2. **使用强密码**：密码建议包含大小写字母、数字和特殊字符，长度至少12位
3. **定期更换密码**：建议每3-6个月更换一次管理员密码
4. **保护 .env 文件**：确保 `.env` 文件不被提交到版本控制系统
5. **HTTPS**：生产环境建议使用 HTTPS 协议

## 故障排除

### 问题：登录后立即退出

**原因**：JWT_SECRET 未配置或配置错误

**解决方案**：
1. 检查 `.env` 文件中是否有 `JWT_SECRET`
2. 确保 `JWT_SECRET` 是一个非空字符串
3. 重启服务

### 问题：无法发送邮件

**原因**：SMTP 配置错误或邮箱未开启 SMTP 服务

**解决方案**：
1. 确认 SMTP 服务器地址和端口正确
2. 检查邮箱是否已开启 SMTP 服务
3. QQ/163 邮箱需要使用授权码而不是登录密码
4. Gmail 需要开启两步验证并使用应用专用密码

### 问题：数据库更新失败

**原因**：数据库连接问题或权限不足

**解决方案**：
1. 检查 `DATABASE_URL` 配置是否正确
2. 确认数据库用户有创建表的权限
3. 尝试手动执行 `npx prisma db push --force-reset`（会清空数据，慎用）

## 回滚

如果升级后遇到问题需要回滚：

1. 使用 git 回滚代码：
```bash
git checkout v1.0.0
```

2. 重新安装依赖：
```bash
npm install
```

3. 如果需要，恢复数据库备份

## 技术支持

如遇到问题，请：
1. 查看日志文件
2. 检查浏览器控制台错误信息
3. 提交 Issue 说明问题详情

